# Install Impeccable Design Skills

Use this when the project needs stronger frontend design guidance for avoiding generic AI-looking UI and weak visual hierarchy.

## Recommended Install

Run this inside the project:

```sh
npx skills add pbakaus/impeccable
```

Refresh later with:

```sh
npx impeccable skills update
```
